name: Keep Supabase Awake

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          curl -s --fail -o /dev/null \
            https://creatorfirst.vercel.app/api/health
